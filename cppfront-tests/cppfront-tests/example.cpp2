#include "example.hxx"

main: (args) -> int = {
    if(args.size() > 1u)
    {
        std::cout << "Hello (name(args[1] as std::string))$\n";
    }
    else
    {
        std::cerr << "error: missing name\n";
        return 1;
    }
}
